export interface Interval {
  rootFreq: number;
  noteFreqs: number[];
  perfectFreqs: number[];
  root: number;
  notes: number[];
  rootName: string;
  noteNames: string[];
  name: string;
  interval: number;
}
